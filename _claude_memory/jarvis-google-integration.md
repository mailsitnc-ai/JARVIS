---
name: jarvis-google-integration
description: JARVIS reads Google Drive + Gmail via OAuth; user must supply their own OAuth client
metadata: 
  node_type: memory
  type: project
  originSessionId: 39333a9b-816b-4c6c-a177-4534fa7206f3
  modified: 2026-09-18T05:33:07.699Z
---

Added 2026-09-14. `core/google.py` (stdlib only): OAuth 2.0 installed-app loopback flow + Drive/Gmail read. `GoogleAuth` stores client_id/secret + refresh/access tokens DPAPI-encrypted in `%APPDATA%/JARVIS/google.json` (via `keystore._dpapi`); `login()` runs a one-shot `http.server` on 127.0.0.1:<port>, opens the browser, exchanges the code; `access_token()` auto-refreshes. `GoogleClient.search_drive/search_gmail` hit the REST APIs (scopes drive.readonly, gmail.readonly). Broker `search_drive`/`search_gmail` gated by new `google` permission, importing core.google lazily inside do() so the sandbox harness (which loads actions.py standalone) still works; dry-run returns a placeholder. Builtin skills `drive_search`/`gmail_search` (trigger on "my drive"/"my gmail"/"emails from" etc.). CLI: `jarvis google setup|login|status|logout`; doctor shows status. understand() has drive/gmail examples.

UPDATE 2026-09-16: user created a Google Cloud project "JARVIS" in their PERSONAL account (skipped school - edu accounts often admin-blocked), enabled Gmail+Drive APIs, made a Desktop OAuth client, ran `google setup` (creds stored). WRITE actions added: `core/google.py` SCOPES now `drive.readonly` + `gmail.modify` (read + labels/archive/trash/drafts; NOT gmail.send, no permanent delete). `GoogleClient.gmail_organize(query, action, label)` (action=archive|read|trash|label) via `_message_ids`/`ensure_label`/`_batch_modify` (batchModify: archive=remove INBOX, read=remove UNREAD, trash=add TRASH+remove INBOX, label=ensure+add). Broker `gmail_organize` gated 'google'; builtin skill `skills/gmail_organize.py` parses NL ("archive emails from X", "label emails from boss as Work", "trash emails older than 60 days", "mark promotions as read"); `organize_files` defers email/inbox requests to it. BECAUSE THE SCOPE CHANGED the user MUST re-run `jarvis google login` to grant write (their prior login was read-only/incomplete: status was "credentials set, not logged in"). Pairs with agenda: "triage my inbox every morning". Tests in test_google.py (mocked). Original setup steps below still apply.

SENDING added 2026-09-18 (user: "I want it to send emails from my personal acc"), commit `bda40fb`. Scope `gmail.send` added; `GoogleClient.gmail_send(to,subject,body)` (RFC822 base64url via users.messages.send) + `my_address()` for "myself". New capability **`email_send` in SENSITIVE** (autonomy does NOT blanket-allow it) -> every send ASKS first, showing recipient+subject+full body (ActionRequest details). Broker `gmail_send`; skill `skills/send_email.py` (composes from a draft file JARVIS wrote, or writes body from the instruction; resolves myself->own address; extracts/generates subject). `gmail_search` now DEFERS compose/send phrasing (`_COMPOSE`) so "write an email to X" no longer searches. Drive-search URL bug fixed (`d0bd543`: urlencode spaces in orderBy/fields). Gmail organize NL parsing fixed (`52d295a`: newer/older-than -> tokens, sender capture stops correctly, label from trailing "as X"). LIVE: archived 300 promotions>30d; labelled+cleaned a test label. **User MUST re-run `jarvis google login` before the first send** (token predates the send scope; broker returns a re-login hint on 403). 221 tests pass.

CONNECTED 2026-09-18: fully logged in and LIVE-VERIFIED (secret starts GOCSPX-, refresh_token present, access token obtained, a real Gmail API search returned results). Scopes drive.readonly + gmail.modify are active. Root cause of the earlier failures: the hidden `getpass` prompt in `google setup` ate the pasted client secret down to 1 char (every login failed "invalid client secret"); fixed by `jarvis google setup --show` (visible entry) + a fingerprint print (commit `3e29492`). Drive search + Gmail organize (archive/read/trash/label) are now usable for real; no daemon restart needed (skills read google.json fresh).

IMPORTANT: cannot be live-demoed without the USER's Google Cloud OAuth client (I can't create one). User must: Cloud Console -> project -> enable Drive+Gmail APIs -> OAuth consent (External, add self as Test user) -> create OAuth client ID type "Desktop app" -> `jarvis google setup` (paste id+secret) -> `jarvis google login`. Verified end-to-end up to the login point: routing -> skill -> "Google isn't connected yet" message; token refresh + result parsing unit-tested with mocks. Read-only for now; sending email / writing Drive would be new scopes + capability. Related: [[jarvis-windows-rebuild]].
